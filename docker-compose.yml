services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: saju
      POSTGRES_USER: saju
      POSTGRES_PASSWORD: saju
    ports:
      - "5432:5432"
    volumes:
      - saju_pgdata:/var/lib/postgresql/data

volumes:
  saju_pgdata:
